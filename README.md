# H5game_0630 V57 No Penalty


## V57 수정사항
- 현재 게임 룰에 맞지 않는 `-10` 패널티 이펙트 리소스를 제거했습니다.
- `assets/fx_penalty.png` 파일을 삭제했습니다.
- `damageCenter` 관련 HTML/CSS를 제거했습니다.
- `lastDamageTime` 잔여 변수를 제거했습니다.
- 게임 룰, 조작, 플레이어 애니메이션, 기존 assets 구조는 변경하지 않았습니다.


## V58 수정사항
- Google Web Font `Tomorrow`를 연동했습니다.
- `<head>`에 Google Fonts preconnect 및 stylesheet 링크를 추가했습니다.
- 기존 `TomorrowLocal` 참조를 Google Font `Tomorrow`로 변경했습니다.
- 게임 로직, 조작, 이미지 리소스, 패널티 제거 상태는 변경하지 않았습니다.


## V59 수정사항
- 보급상자 획득 시 이미지 이펙트 대신 스크린샷 기준 `+1` 텍스트 이펙트가 뜨도록 변경했습니다.
- 콤보 중앙 텍스트를 스크린샷 기준 Tomorrow 폰트 스타일로 조정했습니다.
- 피버 모드 표시를 중앙 `FEVER!` 텍스트 스타일로 변경했습니다.
- 위 3개 표시 요소 외 게임 로직/조작/에셋 구조는 변경하지 않았습니다.


## V60 수정사항
- 피그마 설정 기준으로 콤보 숫자/COMBO/FEVER 텍스트 스타일을 수정했습니다.
- 숫자: Tomorrow SemiBold 72px, 행간 130%, 채우기 #4C75FC, 외곽선 #FFFFFF 4px 느낌으로 조정했습니다.
- COMBO: Tomorrow Bold 25px, 행간 130%, 채우기 #4C75FC, 외곽선 #FFFFFF 4px 느낌으로 조정했습니다.
- FEVER: Tomorrow Bold 62px, 행간 130%, 채우기 #A662EA, 외곽선 #FFFFFF 4px 느낌으로 조정했습니다.
- CSS의 기본 text-stroke가 내부/중앙으로 먹는 문제를 줄이기 위해 다중 text-shadow 방식으로 외부 외곽선처럼 보이게 처리했습니다.
- 위 텍스트 스타일 외 게임 로직/조작/배경/아이템/캐릭터는 변경하지 않았습니다.


## V61 수정사항
- 콤보 숫자/COMBO/FEVER 텍스트의 뒤쪽 컬러 그림자를 제거했습니다.
- 흰색 외곽선 느낌만 남기도록 다중 text-shadow를 정리했습니다.
- 텍스트 컬러, 폰트, 크기, 게임 로직/조작/배경/아이템/캐릭터는 변경하지 않았습니다.


## V62 수정사항
- 콤보 숫자와 `COMBO` 텍스트 사이 간격을 약 10px 더 띄웠습니다.
- `.comboText`의 `margin-top` 값을 `-19px`에서 `-9px`로 조정했습니다.
- 게임 로직, 조작, 배경, 캐릭터, 아이템, 텍스트 컬러/외곽선은 변경하지 않았습니다.


## V63 수정사항
- `FEVER!` 문구가 뜨고 사라지는 동안 콤보 문구가 보이지 않도록 처리했습니다.
- 콤보 판정을 총 획득 수 기준이 아니라 `연속 획득 수` 기준으로 변경했습니다.
- 실제 화면 아래로 보급상자/피버 아이템을 놓치면 콤보가 0으로 초기화됩니다.
- 게임 로직 중 위 콤보 판정 외 조작/배경/캐릭터/아이템/텍스트 스타일은 변경하지 않았습니다.


## V64 수정사항
- 보급상자 획득 시 기존 `fx_pickup.png` 이펙트를 다시 표시하도록 복원했습니다.
- 보급상자 획득 시 `+1` 텍스트도 함께 표시되도록 유지했습니다.
- 아이템이 사라질 때 `fx_pickup.png` 이펙트와 `+1` 텍스트가 동시에 나오도록 처리했습니다.
- 콤보 판정/피버 중 콤보 숨김/폰트 스타일/게임 조작은 V63 기준 그대로 유지했습니다.


## V65 수정사항
- `fx_pickup.png`와 `+1`이 보이지 않던 문제를 수정했습니다.
- 이펙트 위치를 inline `transform` 방식이 아니라 `left/top` 좌표 기준으로 변경했습니다.
- CSS 애니메이션의 `transform`이 좌표값을 덮어써서 화면에 제대로 안 보일 수 있던 문제를 해결했습니다.
- 보급상자/피버 아이템 획득 시 기존 픽업 이펙트와 `+1`이 함께 뜹니다.
- V63/V64의 콤보 판정, 피버 중 콤보 숨김, 폰트 스타일, 조작은 유지했습니다.


## V66 수정사항
- V65에서 게임이 먹통이 되던 JavaScript 문법 오류를 수정했습니다.
- 원인: `collect()` 함수 뒤에 닫는 중괄호가 하나 더 들어가 스크립트가 실행되지 않았습니다.
- 기존 게임플레이를 유지하면서 V63 콤보 판정, 피버 중 콤보 숨김, V65 픽업 이펙트 위치 수정만 유지했습니다.
- Node syntax check(`node --check`)로 스크립트 문법 오류가 없는지 확인했습니다.
